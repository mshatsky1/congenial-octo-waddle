# Changelog

## Unreleased

- Added version command to CLI (`--version` flag)
- Added color support for terminal output (`--color` flag)
- Added configuration file support (congenial.toml)
- Added logging functionality with configurable logger
- Added error handling improvements with custom exceptions
- Added utility functions module (sanitize_name, capitalize_words, validate_name)
- Added comprehensive test coverage for new features
- Updated README with new features and usage examples
- Added example usage script demonstrating package capabilities
- Improved type hints across the codebase
- Enhanced docstrings with detailed descriptions
- Added timezone support to timestamps
- Added multiple greeting styles (standard, casual, formal) with `--style` flag
- Added helper scripts and documentation tweaks.
- Added initial packaging scaffold and tests.
